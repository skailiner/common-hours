# Common Hours release

Production verification and public publication receipts will be recorded here after completion.

The project requires no API key, account or paid runtime. The public version is a static application with a local worker. Rosters remain temporary browser-local data.
